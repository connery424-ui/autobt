# Cloudflare Pages redirects for SolSniper
# Proxy API calls to Railway backend

# API endpoints - UPDATE WITH YOUR ACTUAL RAILWAY DOMAIN
/api/* https://your-railway-backend.railway.app/api/:splat 200

# WebSocket connections (if needed via HTTP)
/ws/* https://your-railway-backend.railway.app/ws/:splat 200

# SPA fallback - serve index.html for all other routes
/* /index.html 200
